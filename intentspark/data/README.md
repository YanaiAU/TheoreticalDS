# Data

Real **SST-5** (Stanford Sentiment Treebank, 5-class) from Hugging Face
[`SetFit/sst5`](https://huggingface.co/datasets/SetFit/sst5).

```bash
python src/download_data.py
```

Produces `sst5_train.csv`, `sst5_validation.csv`, `sst5_test.csv` (gitignored).
